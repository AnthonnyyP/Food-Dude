<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel = "stylesheet" href = "/stylesheets/style.css">
  <title>The Food Dude</title>
</head>
<body>
  <h1>The Food Dude</h1>
    <nav>
        <a href = "/" <%- title === 'Home' ? 'class="active"' : '' %>>Home</a>
        <a href = "../../routes/posts.js">Recipes</a>
        <% if (user) { %>
        <a href=""	<%- title === 'Add a Recipe' ? 'class="active"' : '' %>>Add a Recipe</a>
        <a href="/logout">Logout</a>
        <% } else { %>
          <div>
          <a href="/auth/google" class="login">Login&nbsp;<img src=""></a>
        <% } %>
          </div>
      </nav>
    
    